package com.etdon.winj.facade.assembler;

/**
 * Assembler class responsible for turning unspecific calls (like mov rax,r12) into the correct call based on the
 * provided operands, calling MOV_RM64_R64 or something shouldn't be necessary anymore as a result, just MOV.
 */
public final class Assembler {
}
